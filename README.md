How to use:

    git clone git@github.com:huned/dotfiles && cd dotfiles
    ./install.sh

NOTE:

- `install-scripts/` is deprecated and not used by `install.sh`
- `bin/` is deprecated and not used by `install.sh`

TODO:

- TBD
